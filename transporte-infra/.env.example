# --- credenciales de PostgreSQL que corren en mv-bd ----------------------------
# Esta variable se consume en docker-compose.bd.yml (POSTGRES_PASSWORD).
POSTGRES_PASS=

# --- (opcional) credenciales para MySQL/MongoDB que agregarán P2/P3 ------------
# MYSQL_PASS=
# MONGO_PASS=
