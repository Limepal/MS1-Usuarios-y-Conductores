# Copiar a .env en mv-bd. NUNCA subir el .env al repo.
POSTGRES_PASS=
MYSQL_PASS=
MYSQL_ROOT_PASS=
MONGO_ROOT_PASS=
MONGO_APP_PASS=
# --- docker-compose.prod.yml (mv-prod-a / mv-prod-b) usa POSTGRES_PASS,
# MYSQL_PASS y MONGO_APP_PASS de arriba, más la IP privada de mv-bd:
BD_HOST=10.0.2.85
